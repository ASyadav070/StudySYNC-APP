# Node dependencies
node_modules/

# Environment variables (NEVER commit these!)
.env
client/.env
server/.env

# Log files
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Build outputs (Client)
client/dist/
client/.vite/

# Server uploads (Temporary files)
server/uploads/

# Prisma SQLite database file (if used locally, though you use Supabase)
server/prisma/*.db*

# OS generated files
.DS_Store
Thumbs.db